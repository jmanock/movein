import type { Metadata } from "next";
import { PageHero } from "../components/PageHero";
import { pageMetadata } from "../lib/metadata";
export const metadata: Metadata = pageMetadata("Editorial and Data Policy", "How MoveIn researches, labels, verifies, and corrects utility records.", "/editorial-policy");
export default function EditorialPolicyPage() { return <main id="main-content"><PageHero eyebrow="Trust" title="Editorial and data policy" description="Accuracy includes saying what the data cannot determine." /><section className="section"><article className="shell prose"><h2>Primary sources first</h2><p>Records should cite an official provider, municipal, county, state, or federal source. We do not treat search snippets, directories, or memory as final evidence.</p><h2>No false precision</h2><p>A ZIP match is labeled as primary, possible, address confirmation required, coverage varies, or not verified. Internet results should point to address-level tools unless reliable location data supports a specific claim.</p><h2>Review and correction</h2><p>Verification dates are stored separately from service-area notes. Automated reports flag stale or incomplete data. Material corrections are reviewed against an official source and then imported through the documented data workflow.</p></article></section></main>; }
