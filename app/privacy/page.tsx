import type { Metadata } from "next";
import { PageHero } from "../components/PageHero";
import { pageMetadata } from "../lib/metadata";
export const metadata: Metadata = pageMetadata("Privacy", "How MoveIn handles ZIP searches, emails, corrections, and analytics.", "/privacy");
export default function PrivacyPage() { return <main id="main-content"><PageHero eyebrow="Policy" title="Privacy" description="The ZIP lookup works without an account, email address, or street address." /><section className="section"><article className="shell prose"><p><strong>Last updated:</strong> July 28, 2026</p><h2>ZIP searches</h2><p>MoveIn does not save ZIP searches to its application database or associate them with a name or email. The web server and reverse proxy may retain ordinary short-lived request logs, including IP addresses, for security and operations. The lookup does not collect a street address.</p><h2>Corrections</h2><p>A correction requires a ZIP code, issue type, and description. Provider name, supporting URL, and reply email are optional. Reports are stored separately from verified provider records and never update public data automatically.</p><h2>Analytics and sensitive information</h2><p>No analytics provider is configured. If anonymous aggregate measurement is added later, analytics events will not include form values, exact addresses, email addresses, or full IP addresses. MoveIn does not collect utility account credentials, Social Security numbers, identification documents, payment information, leases, or closing documents.</p><h2>Data sales</h2><p>MoveIn does not sell ZIP searches or personal information. Contact hello@movein.guide with privacy questions.</p></article></section></main>; }
