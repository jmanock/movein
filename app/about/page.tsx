import type { Metadata } from "next";
import { PageHero } from "../components/PageHero";
import { pageMetadata } from "../lib/metadata";
export const metadata: Metadata = pageMetadata("About MoveIn", "MoveIn is an independent utility and local-services discovery tool.", "/about");
export default function AboutPage() { return <main id="main-content"><PageHero eyebrow="About MoveIn" title="A clearer starting point after you get the keys." description="MoveIn helps homeowners and renters find essential services after a move, then confirm the exact address with official sources." /><section className="section"><article className="shell prose"><h2>Florida first</h2><p>The initial pilot covers selected ZIP codes in five Central Florida counties. We gather provider, contact, and service information from official utility and government sources and show when records were checked.</p><h2>ZIP code is the starting point</h2><p>Service territories rarely follow ZIP boundaries, so results may list multiple possibilities and require an address check on an official site.</p><h2>Independent and account-free</h2><p>MoveIn is not a utility provider or government agency. It does not sell utility service and does not require an account or email address for lookups.</p></article></section></main>; }
